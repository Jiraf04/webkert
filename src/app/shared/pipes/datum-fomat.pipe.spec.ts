import { DatumFomatPipe } from './datum-fomat.pipe';

describe('DatumFomatPipe', () => {
  it('create an instance', () => {
    const pipe = new DatumFomatPipe();
    expect(pipe).toBeTruthy();
  });
});
